{% extends 'base.html.twig' %}

{% block title %}Accueil - Géoula Kids{% endblock %}

{% block body %}
<div class="space-y-8 sm:space-y-12">
    {# Welcome Section #}
    <div class="text-center space-y-2">
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold gradient-text">
            Bienvenue sur Géoula Kids!
        </h1>
        <p class="text-base sm:text-lg text-royal-blue/80">
            Ton aventure chassidique futuriste commence ici ✨
        </p>
    </div>

    {# News Slider (Simplified for Twig without React state) #}
    <section>
        <h2 class="text-xl sm:text-2xl font-bold text-royal-blue mb-4 sm:mb-6 flex items-center gap-2">
            <span class="text-vibrant-orange">✨</span> Actualités
        </h2>

        <div class="relative w-full h-64 sm:h-80 md:h-96 overflow-hidden rounded-[30px] glow-border group">
            {# Displaying first item for MVP, could be enhanced with JS for slider #}
            {% set news = newsItems[0] %}
            <div class="relative w-full h-full glass-card-dark overflow-hidden">
                <div class="absolute inset-0 bg-cover bg-center opacity-30 transition-transform duration-700 group-hover:scale-105"
                     style="background-image: url('{{ news.image }}')">
                </div>
                <div class="relative h-full flex flex-col justify-end p-6 sm:p-8 md:p-10">
                    <div class="inline-block px-4 py-2 rounded-[15px] bg-vibrant-orange text-white text-xs sm:text-sm font-bold mb-3 w-fit">
                        {{ news.category }}
                    </div>
                    <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-white mb-3 neon-gold">
                        {{ news.title }}
                    </h2>
                    <p class="text-sm sm:text-base text-white/90 max-w-2xl">
                        {{ news.description }}
                    </p>
                </div>
            </div>
        </div>
    </section>

    {# Quick Access Tiles #}
    <section>
        <h2 class="text-xl sm:text-2xl font-bold text-royal-blue mb-4 sm:mb-6">
            Accès Rapide
        </h2>
        <div class="grid grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
            {% for tile in quickAccessTiles %}
                <a href="{{ path(tile.route) }}" class="group neo-card rounded-[25px] p-6 sm:p-8 text-left transition-all duration-300 hover:scale-105 hover:shadow-2xl block">
                    <div class="w-14 h-14 sm:w-16 sm:h-16 rounded-[20px] bg-gradient-to-br {{ tile.color }} flex items-center justify-center mb-4 group-hover:rotate-6 transition-transform duration-300 text-white text-2xl">
                        {% if tile.icon == 'gamepad' %}🎮{% elseif tile.icon == 'video' %}🎥{% elseif tile.icon == 'book' %}📖{% elseif tile.icon == 'calendar' %}📅{% elseif tile.icon == 'sparkles' %}✨{% elseif tile.icon == 'trophy' %}🏆{% endif %}
                    </div>
                    <h3 class="text-base sm:text-lg font-bold text-royal-blue mb-1">
                        {{ tile.title }}
                    </h3>
                    <p class="text-xs sm:text-sm text-gray-600">
                        {{ tile.description }}
                    </p>
                </a>
            {% endfor %}
        </div>
    </section>

    {# Call to Action #}
    <section class="glass-card rounded-[30px] p-6 sm:p-8 md:p-10 text-center">
        <h3 class="text-xl sm:text-2xl font-bold text-royal-blue mb-3">
            Prêt à commencer ton aventure? 🚀
        </h3>
        <p class="text-sm sm:text-base text-gray-700 mb-6 max-w-2xl mx-auto">
            Complète tes missions quotidiennes, gagne des badges exclusifs et grimpe au sommet du leaderboard!
        </p>
        <a href="{{ path('app_missions') }}" class="puffy-button-primary px-8 py-4 text-lg rounded-[20px] font-medium inline-block">
            Voir mes missions
        </a>
    </section>
</div>
{% endblock %}
