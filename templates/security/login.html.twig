{% extends 'base.html.twig' %}

{% block title %}Connexion - Géoula Kids{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-[80vh]">
    <div class="bg-white p-8 rounded-3xl shadow-clay border-4 border-white w-full max-w-md">
        <div class="text-center mb-8">
            <span class="text-6xl block mb-4">👋</span>
            <h1 class="text-3xl font-bold text-royal-blue">Bienvenue !</h1>
            <p class="text-gray-500">Connecte-toi pour voir tes missions</p>
        </div>

        <form method="post" action="{{ path('app_login') }}">
            {% if error %}
                <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded-r" role="alert">
                    <p class="font-bold">Oups !</p>
                    <p>{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                </div>
            {% endif %}

            <div class="mb-6">
                <label for="username" class="block text-gray-700 font-bold mb-2 ml-2">Ton Pseudo</label>
                <input type="text" value="{{ last_username }}" name="_username" id="username" class="w-full px-4 py-3 rounded-2xl bg-gray-100 border-2 border-transparent focus:border-royal-blue focus:bg-white focus:outline-none transition shadow-inner" autocomplete="username" required autofocus placeholder="Ex: David123">
            </div>

            <div class="mb-8">
                <label for="password" class="block text-gray-700 font-bold mb-2 ml-2">Ton Mot de passe</label>
                <input type="password" name="_password" id="password" class="w-full px-4 py-3 rounded-2xl bg-gray-100 border-2 border-transparent focus:border-royal-blue focus:bg-white focus:outline-none transition shadow-inner" autocomplete="current-password" required placeholder="••••••••">
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button type="submit" class="w-full bg-royal-blue text-white font-bold py-4 rounded-2xl shadow-lg btn-bounce hover:bg-blue-800 transition text-xl">
                C'est parti ! 🚀
            </button>
        </form>

        <div class="mt-6 text-center">
            <a href="{{ path('app_register') }}" class="text-royal-blue font-bold hover:underline">Pas encore inscrit ? Crée ton compte ici !</a>
        </div>
    </div>
</div>
{% endblock %}
