{% extends 'base.html.twig' %}

{% block title %}Vidéos - Géoula Kids{% endblock %}

{% block body %}
<div class="space-y-8 max-w-7xl mx-auto">
    {# Header #}
    <div class="text-center space-y-2">
        <h1 class="text-3xl sm:text-4xl font-bold gradient-text flex items-center justify-center gap-3">
            <span class="text-vibrant-orange">🎥</span>
            Vidéos Géoula
        </h1>
        <p class="text-sm sm:text-base text-gray-600">
            Regarde des vidéos passionnantes sur la Torah et la Géoula !
        </p>
    </div>

    {# Videos Grid (Centered) #}
    <div class="flex flex-wrap justify-center gap-6">
        {% for video in videos %}
            <div class="glass-card rounded-[25px] overflow-hidden group hover:shadow-2xl transition-all duration-300 flex flex-col w-full max-w-2xl">
                {# Video Player / Thumbnail #}
                <div class="relative aspect-video bg-black">
                    <iframe
                        src="{{ video.url }}"
                        title="{{ video.title }}"
                        class="w-full h-full absolute inset-0"
                        frameborder="0"
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                        allowfullscreen>
                    </iframe>
                </div>

                {# Content #}
                <div class="p-5 flex flex-col flex-grow text-center">
                    <div class="flex justify-center items-center gap-3 mb-3">
                        <span class="px-3 py-1 rounded-full bg-blue-100 text-royal-blue text-xs font-bold">
                            {{ video.category }}
                        </span>
                        <span class="text-xs text-gray-500 flex items-center gap-1">
                            ⏱️ {{ video.duration }}
                        </span>
                    </div>

                    <h3 class="text-xl font-bold text-royal-blue mb-2 group-hover:text-vibrant-orange transition-colors">
                        {{ video.title }}
                    </h3>

                    <p class="text-sm text-gray-600">
                        {{ video.description }}
                    </p>
                </div>
            </div>
        {% else %}
            <div class="w-full text-center py-12">
                <p class="text-gray-500 text-lg">Aucune vidéo disponible pour le moment.</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
